# dns_request_parser
1. Setup environment variable
2. Copy the directory to examples/
3. Make
4. ./build/dns_request_parser
5. Run "dig xxx.com @x.x.x.x" on client. x.x.x.x is DPDK interface IP address.
6. You will see the application print the domain.
